var yearlyFestivals={};
function loadTenant(a){admin={...a};const legacy=a.email==='manager@msme.com';people=read(tenantKey('people'),legacy?read('msme-people',people):[]).map(fix);exPeople=read(tenantKey('ex'),legacy?read('msme-ex-people',exPeople):[]).map(fix);temporaryWorkers=read(tenantKey('temporary'),[]).map(fix);festivalCalendar=read(tenantKey('festivals'),defaultFestivals);attendanceLog=read(tenantKey('attendance'),[]);yearlyFestivals=read(tenantKey('festival-years'),{});festivalYear=festivalCalendar[0]?+festivalCalendar[0][0].slice(0,4):new Date().getFullYear();yearlyFestivals[festivalYear]=festivalCalendar}
function save(){yearlyFestivals[festivalYear]=festivalCalendar;localStorage.setItem(tenantKey('people'),JSON.stringify(people));localStorage.setItem(tenantKey('ex'),JSON.stringify(exPeople));localStorage.setItem(tenantKey('temporary'),JSON.stringify(temporaryWorkers));localStorage.setItem(tenantKey('festivals'),JSON.stringify(festivalCalendar));localStorage.setItem(tenantKey('festival-years'),JSON.stringify(yearlyFestivals));localStorage.setItem(tenantKey('attendance'),JSON.stringify(attendanceLog));localStorage.setItem('msme-accounts',JSON.stringify(tenantAccounts))}
function switchYear(delta){yearlyFestivals[festivalYear]=festivalCalendar;festivalYear+=delta;if(!yearlyFestivals[festivalYear])yearlyFestivals[festivalYear]=festivalCalendar.map(f=>[festivalYear+f[0].slice(4),f[1]]);festivalCalendar=yearlyFestivals[festivalYear];save();render()}
function weeklyLeave(){const now=new Date(),start=new Date(now);start.setDate(now.getDate()-6);return people.filter(p=>p.status==='On leave'||p.status==='Absent').map(p=>({p,days:attendanceLog.filter(d=>new Date(d.date+'T00:00:00')>=start).filter(d=>d.records.find(r=>r.id===p.id&&r.status!=='Present')).length}))}
function dashboard(){const present=people.filter(p=>p.status==='Present').length,total=people.length,pct=total?Math.round(present/total*100):0,temp=temporaryWorkers.filter(p=>elapsed(p)>=170),leave=weeklyLeave();root.innerHTML=`<div class="page-heading"><div><div class="eyebrow">ADMIN CONTROL CENTRE</div><h1>Good morning, ${esc(admin.name)}.</h1><p>Year-wise planning, attendance and workforce alerts.</p></div></div><div class="metrics">${card('Worker','◉','workers')}${card('Staff','◈','staff')}${card('Entrepreneur','♔','entrepreneurs')}</div><section class="panel health" style="margin-top:18px"><div class="panel-header"><h2>Attendance health</h2><button class="text-button" data-view="attendance">Attendance calendar →</button></div><div class="health-line"><b>${present} / ${total} on duty</b><span>${pct}%</span></div><div class="progress"><i style="width:${pct}%"></i></div><div class="health-line"><span>On leave</span><b>${people.filter(p=>p.status==='On leave').length}</b></div><div class="health-line"><span>Absent</span><b>${people.filter(p=>p.status==='Absent').length}</b></div></section>${temp.length?`<div class="temporary-banner"><b>Temporary-worker notification:</b> ${temp.map(p=>esc(p.name)+' - '+tempNote(p)).join(' · ')} <button class="text-button" data-view="temporary">Review</button></div>`:''}<div class="dashboard-grid"><section class="panel"><div class="panel-header"><h2>Weekly leave summary</h2><span class="tag warning">Last 7 days</span></div>${leave.map(x=>`<div class="notice"><span class="notice-icon">!</span><div><strong>${esc(x.p.name)} · ${esc(x.p.role)}</strong><span>${x.days} recorded leave / absence day${x.days!==1?'s':''} this week</span></div></div>`).join('')||'<p class="empty">No weekly leave or absence recorded.</p>'}</section><section class="panel"><div class="panel-header"><h2>Individual record storage</h2></div><p style="font-size:12px;color:var(--muted);line-height:1.6;margin:0">Every status change and timing update is saved in the daily attendance register for each Worker, Staff member and Entrepreneur. Open Attendance Calendar to review each person by date, month or year.</p></section></div><h2 class="section-title">Festival calendar - ${festivalYear} <button class="text-button" id="previous-festival-year">← Previous year</button> <button class="text-button" id="next-festival-year">Next year →</button> <button class="text-button" id="edit-calendar">Edit this year</button></h2><section class="panel"><div class="calendar-grid">${Array.from({length:12},(_,i)=>new Date(festivalYear,i,1).toLocaleString('en',{month:'long'})).map((m,i)=>`<div class="calendar-month"><b>${m} ${festivalYear}</b>${festivalCalendar.filter(f=>+f[0].slice(5,7)===i+1).map(f=>`<span>${esc(f[0].split('-').reverse().join('-'))} · ${esc(f[1])}</span>`).join('')||'<small>No festival</small>'}</div>`).join('')}</div></section>`}
document.addEventListener('click',e=>{if(e.target.closest('#next-festival-year'))switchYear(1);if(e.target.closest('#previous-festival-year'))switchYear(-1)});
